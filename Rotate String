class Solution {
    public boolean rotateString(String s, String goal) {
        if(s.length() != goal.length())
           return false;
        else{
            String samp = goal + goal;
            if(samp.contains(s))
            return true;
            else
            return false;
        }
        
        
    }
}
